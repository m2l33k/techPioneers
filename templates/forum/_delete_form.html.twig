<form method="post" action="{{ path('app_forum_delete', {'Id_Forum': forum.Id_Forum}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forum.Id_Forum) }}">
    <button class="btn">Delete</button>
</form>
